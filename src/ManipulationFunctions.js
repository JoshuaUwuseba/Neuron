//These functions take a given data structure manipulate it to produce a new data structure.

export function searchFunction(data, query){
    if(data == ""){
        return data
    }
    else{
        // implement the search function on the given data structure and return the result.
        return query
    }
}

export function filterFunction(data, query){
    if(data == ""){
        return data
    }
    else{
        // implement the search function on the given data structure and return the result.
        return query
    }
}

export function sortFunction(data, query){
    if(data == ""){
        return data
    }
    else{
        // implement the search function on the given data structure and return the result.
        return query
    }
}
